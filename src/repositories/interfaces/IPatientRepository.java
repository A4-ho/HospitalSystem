package src.repositories.interfaces;

import src.models.Patient;

public interface IPatientRepository extends CrudRepository<Patient> {
    // Additional methods for patients (if needed) can go here
}
